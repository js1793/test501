<%= form_for(@supervisor) do |f| %>
  <% if @supervisor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supervisor.errors.count, "error") %> prohibited this supervisor from being saved:</h2>

      <ul>
      <% @supervisor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :.ideagroup_id %><br>
    <%= f.number_field :.ideagroup_id %>
  </div>
  <div class="field">
    <%= f.label :coach_id %><br>
    <%= f.number_field :coach_id %>
  </div>
  <div class="field">
    <%= f.label :seniority %><br>
    <%= f.number_field :seniority %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
