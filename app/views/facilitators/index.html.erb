<p id="notice"><%= notice %></p>

<h1>Listing Facilitators</h1>

<table>
  <thead>
    <tr>
      <th>.gitignoregroup</th>
      <th>Leader</th>
      <th>Role</th>
      <th>Title</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @facilitators.each do |facilitator| %>
      <tr>
        <td><%= facilitator..gitignoregroup_id %></td>
        <td><%= facilitator.leader_id %></td>
        <td><%= facilitator.role_id %></td>
        <td><%= facilitator.title_id %></td>
        <td><%= link_to 'Show', facilitator %></td>
        <td><%= link_to 'Edit', edit_facilitator_path(facilitator) %></td>
        <td><%= link_to 'Destroy', facilitator, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Facilitator', new_facilitator_path %>
